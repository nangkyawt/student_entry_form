import { NrcExistsPipe } from './nrc-exists.pipe';

describe('NrcExistsPipe', () => {
  it('create an instance', () => {
    const pipe = new NrcExistsPipe();
    expect(pipe).toBeTruthy();
  });
});
